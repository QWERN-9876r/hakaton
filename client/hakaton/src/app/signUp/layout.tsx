import type { Metadata } from 'next'
import { MainLayout } from '@/standartLayout'

export const metadata: Metadata = {
    title: 'Регистрация - Family Wallet',
    description: 'Family Wallet',
}

export default MainLayout
